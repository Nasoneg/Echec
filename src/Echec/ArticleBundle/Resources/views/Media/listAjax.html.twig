{% if(pagination|length) %}
    <ul class="phototiles isotope js-media-content">
      {% include "EchecArticleBundle:Media:listAjaxElements.html.twig"%}
    </ul>
    <div class="pagination--styled">{{ knp_pagination_render(pagination) }}</div>
{% else %}
    {# Empty state #}
    <ul class="phototiles isotope js-media-content">
    </ul>
{% endif %}