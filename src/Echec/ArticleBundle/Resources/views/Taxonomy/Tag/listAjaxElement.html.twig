{% if level is not defined %}
    {% set level = 0 %}
{% endif %}
<tr>
    <td>{{ taxonomy|categoryLevelSlash }}</td>
    <td>{{ taxonomy.term.slug }}</td>
    <td>{{ taxonomy.description }}</td>
    <td>{{ taxonomy.count | default('0') }}</td>
    <td>
        <a href="#" class="js-delete-object" data-title="Supprimer tag" data-text="Êtes vous sur de vouloir supprimer ce tag ?" data-href="{{ path('ed_blog_tag_delete', {'slug': taxonomy.term.slug} ) }}" data-toggle="modal" data-target="#delete"><i class="fa fa-times"></i> Supprimer</a>
        <a href="{{ path('ed_blog_tag_edit', {'slug': taxonomy.term.slug}) }}" class="js-edit-{{ taxonomy.term.slug }}"><i class="fa fa-pencil"></i> Editer</a>
    </td>
</tr>